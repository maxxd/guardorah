# guardorah

Chrome extension to prevent Jira's edit-on-click of rich text fields that cause the UI to shift and stutter when trying to select text

To install:

- Clone this repository
- Enable Developer mode
- Select "Load Unpacked"
- Select the local directory where this is cloned
